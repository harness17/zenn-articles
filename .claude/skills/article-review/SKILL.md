---
name: article-review
description: Zenn記事の公開前チェックを行うスキル。文体ルール違反、必須要素、守秘義務、フロントマターを網羅的に検査する。「記事をレビュー」「公開前チェック」「文体チェック」と言われたとき、または記事の `published` を `true` に変える前に発動する。
---

# article-review

`articles/<slug>.md` を公開前に検査する。指摘は表形式で出し、修正は自動でしない（ユーザーがレビューしてから直す）。

## 起動条件

- ユーザーが「記事をレビュー」「公開前チェック」「推敲」と言ったとき
- フロントマターを `published: true` に変更しようとしているとき
- `/article-review` で呼ばれたとき

## チェック項目

### 1. フロントマター

| 項目 | 必須/推奨 | チェック内容 |
|------|---------|------------|
| title | 必須 | 30〜40字、検索キーワード含む |
| emoji | 必須 | 設定済みか |
| type | 必須 | `tech` または `idea` |
| topics | 必須 | 1〜5個、英小文字 |
| published | 必須 | 公開直前のみ `true` |

### 2. 文体ルール違反語スキャン

以下の語を grep し、ヒットしたら行番号と該当箇所を表示：

```
NG語リスト（@.claude/rules/writing-style.md と同期）:
- 素晴らしい / 驚くべき / 画期的 / 魔法のように
- いかがでしたでしょうか / いかがだったでしょうか
- ぜひ参考にしてみてください / ご参考になれば幸いです
- 〜とされている / 〜と言われている（一次情報なら断言する）
- 皆さんも試してみてください
```

`〜ing 連鎖`（同じ段落内で「〜することで」が3回以上）も検出する。

### 3. 必須要素チェック

| 要素 | 検出方法 |
|------|---------|
| GitHub リポジトリへのリンク | `github.com/harness17/` を本文に含むか |
| 実コード（5行以上） | コードブロックの最大行数を計測 |
| 自分の体験・失敗例 | 「実際に〜した」「〜で失敗した」等の体験表現があるか（手動確認） |
| 文字数 | 2000字以上か |

### 4. 守秘義務チェック

以下のキーワードに該当する固有名詞が含まれていないか：

- 過去勤務先名（トランスワード、プロアクティブ）
- 顧客名（ボートレース下関、旅行業の固有社名 等）
- 同僚・関係者の実名

該当があったら必ず指摘する。一般化を提案する。

### 5. 構成チェック

- 「はじめに」「まとめ」セクションがあるか
- 見出しが3〜5個か
- コードブロックに言語指定があるか（` ``` ` だけのブロックは検出）

## 出力フォーマット

```
## 公開前チェック結果: <ファイル名>

### フロントマター
✅ title (32字, OK)
✅ topics: ["sqlserver", "csharp"] (2個)
⚠️ published: true ← 公開直前なら正しい

### 文体ルール違反
⚠️ Line 42: 「いかがでしたでしょうか」 → 別の締め文に
⚠️ Line 87: 「〜することで」が3連続

### 必須要素
✅ GitHub リンク (github.com/harness17/DevNext)
✅ コードブロック最大 12行
⚠️ 文字数 1850字 ← 2000字未満。本論を厚くするか「軽め記事」と割り切る

### 守秘義務
✅ 業務固有名詞なし

### 構成
✅ はじめに / まとめ あり
⚠️ Line 60: コードブロックに言語指定なし → ` ```sql ` 推奨

### 公開可否判断
🟡 軽微な指摘あり。修正後に再レビュー推奨。
```

## やらないこと

- 自動修正（ユーザーが判断する）
- 内容の意味的レビュー（事実誤認は別途、アドバイザーモデルで確認する）

## 完了条件

- すべてのチェック項目について結果を報告した
- 公開可否判断（🟢 / 🟡 / 🔴）を提示した
